export type Inputs = {
  email: string;
  passcode: string;
};

export type SignupInputs = {
  phoneNumber: number;
  verifyPhoneNumber: string;
  firstName: string;
  lastName: string;
  email: string;
  passcode: string;
  transcationPin: string;
};
